[pytest]
# tell pytest where our tests are
testpaths = ./tests
addopts = --ignore=tests/partitioning/evidence

markers =
    slow: long-running tests
    density_matrix: density-matrix project regression suite


